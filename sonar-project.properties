sonar.projectKey=tanks-api
# Source
sonar.sources=.
# Where to find tests file, also src
# sonar.tests=src
# But we get specific here
# We don't need to exclude it in sonar.sources because it is automatically taken care of
# sonar.test.inclusions=src/**/*.spec.ts,src/**/*.spec.tsx,src/**/*.test.ts,src/**/*.test.tsx

sonar.javascript.lcov.reportPaths=./coverage/lcov.info
sonar.inclusions = src/**/*.ts
; sonar.exclusions=prisma/**,jest.config.js,**/*.spec.ts,**/*.test.ts,**/main.ts,**/*.entity.ts,**/index.ts,**/*.module.ts,**/*database.client.ts,s**/*.dto.ts,**/*.type.ts,**/*.spec.ts,libs/tests/**
sonar.verbose=false
